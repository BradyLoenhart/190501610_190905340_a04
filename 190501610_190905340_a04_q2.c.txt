#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <ctype.h>
#include <string.h>
#define MAX_ID 100

struct Node *front;
struct Node *block;
struct Node *tmp;

struct Node {
	int availableSpace;
	struct Node *nextNode;
	char pid[MAX_ID];
	int startAdd;
	int endAdd;

};

char req[6];
char proccess[3];
int lastAddSpace;
int requestedSpace;